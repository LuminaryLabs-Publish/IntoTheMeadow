# Turn Ledger

**Repository:** `LuminaryLabs-Publish/IntoTheMeadow`  
**Timestamp:** `2026-07-11T02-20-44-04-00`  
**Mode:** documentation-only breakdown

## Selection

All ten accessible Publish repositories were reviewed. `TheCavalryOfRome` was excluded. All nine eligible repositories were tracked and had root `.agent` state. `IntoTheMeadow` was the oldest eligible central ledger entry.

## Work completed

```txt
reviewed AGENTS.md
reviewed web host and boot path
reviewed game state, snapshot, and render-plan source
reviewed browser editor bridge
reviewed Node headless environment
reviewed DSK registry and descriptor factory
counted declared and required kits
mapped all declared services
separated registry declarations from runtime implementations
defined registry truth and consumer proof contracts
updated root .agent documents
added timestamped audits
updated central ledger and change log
```

## Main finding

The registry contains 43 local IDs and one external ID, for 44 declared kits total. Prior docs overstated the local count. Required-list membership currently drives `active-v0.1`, but no ledger proves implementation, import, invocation, output, or consumer use.

The required `meadow-webgl-renderer-v2-kit` has no explicit label or services row and therefore receives generic fallback services that do not describe the actual renderer.

## Runtime impact

```txt
runtime source changed: no
visual behavior changed: no
gameplay changed: no
deployment changed: no
```
