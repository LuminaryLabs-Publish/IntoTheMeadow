# Turn ledger: 2026-07-13T05-31-58-04-00

## Summary

Selected `LuminaryLabs-Publish/IntoTheMeadow` after comparing all ten accessible Publish repositories, excluding `TheCavalryOfRome`, confirming nine central ledgers and nine root `.agent` folders, and applying the oldest-documented fallback rule.

The source audit found that `startWebHost()` exposes `stop()` and `start()` but does not own terminal retirement. `stop()` only flips a boolean; fatal handling does the same. Neither path cancels an identified RAF, disposes the WebGL renderer, disposes the editor bridge, detaches bridge listeners, revokes `GameHost`, or publishes a typed lifecycle result.

## Plan ledger

**Goal:** distinguish pause/resume from terminal retirement and make every host participant retire exactly once under one lifecycle generation.

- [x] Compare the current Publish repository list with the central ledger.
- [x] Exclude `LuminaryLabs-Publish/TheCavalryOfRome`.
- [x] Confirm all nine eligible repositories have root `.agent` state.
- [x] Select only `LuminaryLabs-Publish/IntoTheMeadow` as the oldest eligible ledger entry.
- [x] Identify the interaction loop, domains, kits, and services.
- [x] Trace RAF, fatal, renderer, bridge, and global-capability ownership.
- [x] Define the web-host lifecycle retirement authority boundary.
- [x] Add the timestamped tracker and audit family.
- [x] Refresh required root `.agent` documents.
- [ ] Runtime lifecycle implementation and executable fixtures remain future work.

## Source paths reviewed

```txt
README.md
package.json
index.html
src/boot/boot-game.js
src/hosts/web-host.js
src/boot/expose-game-host.js
src/editor/install-editor-bridge.js
src/game/create-into-the-meadow-game.js
src/renderers/meadow-webgl-renderer-v2-compatible.js
src/renderers/meadow-webgl-renderer-v2.js
src/content/game-manifest.js
src/boot/install-dsks.js
src/dsks/index.js
tests/static-smoke.mjs
.agent/START_HERE.md
.agent/kit-registry.json
```

## Findings

1. `stop()` sets `stopped = true` but does not call `cancelAnimationFrame`, `renderer.dispose()`, or `editorBridge.dispose()`.
2. `showFatal()` also only marks the host stopped and projects an error.
3. The renderer already exposes `dispose()` and the editor bridge already exposes `dispose()`, but the host does not compose them into a terminal transaction.
4. `exposeGameHost()` installs `globalThis.GameHost` with no revoke or generation boundary.
5. Repeated `startWebHost()` calls can overwrite global bridges while predecessor listeners and GPU resources remain live.
6. Existing static proof checks construction markers, not stop, restart, fatal cleanup, duplicate boot, listener detachment, or global revocation.

## Validation boundary

Documentation only. Runtime JavaScript, gameplay, rendering, provider loading, packages, dependencies, and deployment were not changed. No browser lifecycle, duplicate-start, fatal-cleanup, listener-leak, GPU-retirement, or Pages fixture was run.
