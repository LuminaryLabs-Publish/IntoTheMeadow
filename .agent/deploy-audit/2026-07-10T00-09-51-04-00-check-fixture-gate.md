# Deploy Audit: Check Fixture Gate

**Repository:** `LuminaryLabs-Publish/IntoTheMeadow`

**Timestamp:** `2026-07-10T00-09-51-04-00`

## Existing package scripts

`package.json` exposes:

```txt
npm run check
npm test
```

`npm run check` currently runs:

```txt
node tests/static-smoke.mjs
node tests/dsk-registry-smoke.mjs
node tests/render-plan-smoke.mjs
node tests/renderer-v2-smoke.mjs
node tests/deterministic-scene-smoke.mjs
```

## Current validation status

This pass was documentation-only.

```txt
runtime source changed: no
package scripts changed: no
branch created: no
pull request created: no
npm run check: not run
npm test: not run
browser smoke: not run
DOM-free fixture: not run because new proof files do not exist yet
```

## Required next gate

Add these tests before changing visuals or browser interaction:

```txt
tests/render-consumption-ledger-smoke.mjs
tests/grass-consumption-ledger-smoke.mjs
tests/action-result-fixture-smoke.mjs
```

Then wire them into:

```txt
npm run check
```

## Deployment finding

The repo has a useful check script, but it does not yet guard the renderer snapshot proof, grass parity proof, or objective/action proof rows.

The next deploy-safe cut should make those rows pass locally before any visible route change.
