# Turn Ledger: IntoTheMeadow Reset Session Replay Audit

**Timestamp:** `2026-07-14T09-58-25-04-00`  
**Repository:** `LuminaryLabs-Publish/IntoTheMeadow`  
**Branch:** `main`

## Summary

Selected IntoTheMeadow by the oldest eligible synchronized-ledger rule after comparing all 11 Publish repositories, excluding Cavalry, and finding no new, missing, undocumented or runtime-ahead eligible repository.

The audit traced `runtime.reset` through the browser editor bridge, headless environment and core game. Reset recreates frame-zero state with the same session identity while browser render evidence, headless capture baselines and scheduler ownership are not settled through one transaction.

## Plan ledger

**Goal:** record one complete, reproducible documentation pass for reset participant ownership and replay evidence.

- [x] Inspect current central selection state.
- [x] Inspect `game-state.js`, `create-into-the-meadow-game.js`, `web-host.js`, `expose-game-host.js`, `install-editor-bridge.js`, the headless environment and command smoke.
- [x] Identify browser/headless reset divergence.
- [x] Preserve all 44 kit surfaces and offered services.
- [x] Add architecture, render, gameplay, interaction, reset/replay, deploy and central-sync audits.
- [x] Refresh root `.agent` state.
- [x] Push only to `main`.
- [ ] Implement runtime authority and executable fixtures later.

## Finding

```txt
reset state identity: reused
browser reset participants: game state only
headless reset participants: game state, time and enhancer
browser lastPlan/lastRender: retained
headless lastCapture: retained
RAF/manual tick admission barrier: absent
reset command/result identity: absent
first reset-session frame acknowledgement: absent
```

## Change boundary

Documentation only. No runtime, test, dependency, workflow or deployment behavior changed.